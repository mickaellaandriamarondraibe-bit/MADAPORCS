package com.madaporc.service;

import org.springframework.stereotype.Service;

/**
 * Service placeholder conforme au PDF.
 * Remplacer progressivement les commentaires par les vraies méthodes.
 */
@Service
public class AdminUtilisateurService {


    // String verifierPermissionAdmin(Long utilisateurId)
    // List<Utilisateur> findAllUtilisateurs()
    // String creer(UtilisateurDTO dto)
    // String modifier(Long id, UtilisateurDTO dto)
    // String changerRole(Long utilisateurId, Long roleId)
    // String desactiverUtilisateur(Long utilisateurId)
    // String activerUtilisateur(Long utilisateurId)
    // String affecterPermissions(RolePermissionDTO dto)
    // Map<Role,List<Permission>> getRolesPermissions()

}
