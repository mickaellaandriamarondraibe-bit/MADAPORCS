package com.madaporc.service;

import org.springframework.stereotype.Service;

/**
 * Service placeholder conforme au PDF.
 * Remplacer progressivement les commentaires par les vraies méthodes.
 */
@Service
public class SuiviSanitaireService {


    // List<SuiviSanitaire> rechercherSuivis(Long statutId, LocalDate debut, LocalDate fin)
    // String ajouter(SuiviSanitaireDTO dto, Long utilisateurId)
    // String modifier(Long id, SuiviSanitaireDTO dto)
    // String modifierStatut(Long suiviId, Long statutId)
    // BigDecimal calculerTauxGuerison(LocalDate debut, LocalDate fin)
    // BigDecimal calculerTauxMortalite(LocalDate debut, LocalDate fin)
    // List<SuiviSanitaire> listerAlertesSanitaires()
    // String verifierNombreMalades(Long lotId, Integer nombreMalades)

}
