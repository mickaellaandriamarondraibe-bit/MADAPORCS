package com.madaporc.service;

import org.springframework.stereotype.Service;

/**
 * Service placeholder conforme au PDF.
 * Remplacer progressivement les commentaires par les vraies méthodes.
 */
@Service
public class VaccinationService {


    // List<Vaccination> rechercherVaccinations(Long lotId, Long reproducteurId, LocalDate debut, LocalDate fin)
    // String ajouter(VaccinationDTO dto, Long utilisateurId)
    // LocalDate calculerDateRappel(Long vaccinId, LocalDate dateVaccination)
    // String verifierCibleVaccination(Long lotId, Long reproducteurId)
    // List<Vaccination> listerVaccinationsAVenir(LocalDate dateLimite)

}
