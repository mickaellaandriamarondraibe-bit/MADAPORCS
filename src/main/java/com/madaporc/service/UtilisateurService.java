package com.madaporc.service;

import org.springframework.stereotype.Service;

/**
 * Service placeholder conforme au PDF.
 * Remplacer progressivement les commentaires par les vraies méthodes.
 */
@Service
public class UtilisateurService {


    // String connecter(LoginDTO dto, HttpSession session)
    // boolean verifierMotDePasse(String brut, String hash)
    // Optional<Utilisateur> findByEmail(String email)
    // boolean utilisateurActif(Utilisateur utilisateur)
    // String redirectionSelonRole(Utilisateur utilisateur)

}
