package com.madaporc.service;

import org.springframework.stereotype.Service;

/**
 * Service placeholder conforme au PDF.
 * Remplacer progressivement les commentaires par les vraies méthodes.
 */
@Service
public class DepenseService {


    // List<Depense> rechercherDepenses(LocalDate debut, LocalDate fin, Long categorieId)
    // String creer(DepenseDTO dto, Long utilisateurId)
    // String modifier(Long id, DepenseDTO dto)
    // BigDecimal calculerTotalDepenses(LocalDate debut, LocalDate fin)
    // String validerDepense(DepenseDTO dto)
    // void prepareDepenseFormModel(Model model, Long id)

}
